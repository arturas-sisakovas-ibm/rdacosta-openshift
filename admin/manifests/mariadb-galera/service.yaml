apiVersion: v1
kind: Service
metadata:
  name: mariadb-galera
  labels:
    app: mariadb-galera
spec:
  clusterIP: None
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  selector:
    app: mariadb-galera

