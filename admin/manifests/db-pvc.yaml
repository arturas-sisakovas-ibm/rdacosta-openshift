apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: world-db
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-storage 
  volumeMode: Filesystem
