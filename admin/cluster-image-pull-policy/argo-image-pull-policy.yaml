apiVersion: config.openshift.io/v1
kind: Image
metadata:
  name: cluster
  annotations: 
    argocd.argoproj.io/sync-options: ServerSideApply=true,Validate=false
spec:
  registrySources:
    allowedRegistries:
      - registry.redhat.io
      - registry.access.redhat.com
      - registry.ocp.example.com
      - quay.io/rdacosta
