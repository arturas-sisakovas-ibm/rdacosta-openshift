apiVersion: velero.io/v1
kind: Backup
metadata:
  name: backup-webservers
  namespace: openshift-adp
  labels:
    velero.io/storage-location: oadp-backup-1
spec:
  includedNamespaces:
    - webservers
  storageLocation: oadp-backup-1
  snapshotVolumes: true
