apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: html-data
  namespace: webservers
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeMode: Block
