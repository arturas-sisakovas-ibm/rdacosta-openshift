apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: webservers
spec:
  minAvailable: 1
  selector:
    matchExpressions:
      - key: kubevirt.io/domain
        operator: In
        values:
          - web1
          - web2

