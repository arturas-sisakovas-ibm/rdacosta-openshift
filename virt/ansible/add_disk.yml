---
- name: Add disk to VM 
  hosts: localhost
  gather_facts: false
  become: false
  force_handlers: true

  tasks:

    - name: Ensure disk is added
      redhat.openshift.k8s:
        template: templates/add_disk.j2
