spec:
  template:
    spec:
      volumes:
        - name: secret-volume
          secret:
            secretName: vms-secret
      domain:
        devices:
          disks:
            - name: secret-volume
              disk:
                bus: virtiofs
