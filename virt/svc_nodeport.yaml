---
apiVersion: v1
kind: Service
metadata:
  name: ssh-REPLACE_ME
spec:
  type: NodePort
  ports:
  - name: ssh
    port: 22
    protocol: TCP
    targetPort: 22
  selector:
    kubevirt.io/domain: REPLACE_ME

