---
apiVersion: v1
kind: Service
metadata:
  annotations:
    metallb.universe.tf/address-pool: gls-metallb-ipaddresspool
  name: REPLACE_ME
spec:
  ports:
  - name: ssh
    port: 22
    protocol: TCP
    targetPort: 22
  selector:
    kubevirt.io/domain: REPLACE_ME
  type: LoadBalancer

